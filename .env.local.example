# Environment Variables Template

# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to git!

# Appwrite Configuration
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_APPWRITE_DATABASE_ID=your_database_id_here
NEXT_PUBLIC_APPWRITE_INVOICES_COLLECTION_ID=your_invoices_collection_id_here
NEXT_PUBLIC_APPWRITE_USERS_COLLECTION_ID=your_users_collection_id_here

# Instructions:
# 1. Go to https://cloud.appwrite.io and create a new project
# 2. Copy your Project ID from the project settings
# 3. Create a new database and copy the Database ID
# 4. Create the "invoices" collection and copy the Collection ID
# 5. Create the "users" collection and copy the Collection ID
# 6. Replace the placeholder values above with your actual IDs
# 7. Save this file as .env.local in the root directory
